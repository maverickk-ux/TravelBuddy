<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Budget Input -->
        <EditText
            android:id="@+id/editTextBudget"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter your budget (₹)"
            android:inputType="number"/>

        <!-- Start Date -->
        <EditText
            android:id="@+id/editTextStartDate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Start Date (dd/mm/yyyy)"
            android:focusable="false"/>

        <!-- End Date -->
        <EditText
            android:id="@+id/editTextEndDate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="End Date (dd/mm/yyyy)"
            android:focusable="false"/>

        <!-- Travel Type Selection -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Type of Places to Visit:"
            android:layout_marginTop="16dp"/>

        <CheckBox android:id="@+id/checkboxNature" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="Nature"/>
        <CheckBox android:id="@+id/checkboxHistorical" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="Historical"/>
        <CheckBox android:id="@+id/checkboxAdventure" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="Adventure"/>
        <CheckBox android:id="@+id/checkboxShopping" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="Shopping"/>

        <!-- Submit Button -->
        <Button
            android:id="@+id/buttonGeneratePlan"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Generate Itinerary"
            android:layout_marginTop="24dp"/>

    </LinearLayout>
</ScrollView>
